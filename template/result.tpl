<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Termination of Transfer</title>

    <style>
      body {
        font-family: sans-serif;
      }
      th {
        text-align: left;
      }
    </style>

  </head>
  <body>
    <div class="container">

      <h2 style="align=center">Termination of Transfer Information Sheet<br>
        Creative Commons</h2>

      <hr class="span12">

      <p>This is an information sheet generated by the Creative Commons Returning Author’s Rights Tool. It is not a termination notice or a legal document. It is also not legal advice or a substitute for legal advice.<br>
        <b><i>This is a beta information sheet produced for testing purposes only.</i></b><br>
        For more information, check out the FAQ at http://labs.creativecommons.org/faq.php .</p>

      <p><b>Information Sheet Date:</b> {$date}</p>

      <h3>Conclusion</h3>
      <h4>{$conclusion_title}<br>
        {$conclusion_subtitle}</h4>
      <p>{$conclusion_description}</p>

      {if $flags}<h3>Flags</h3>{/if}
      {foreach $flags as $flag}
        <h4>{$flag[0]}</h4>
        <p>{$flag[1]}</p>
      {/foreach}

      <div>
        {if $details}
        <h3>Details</h3>
        <table style="width: 50%">
          <tbody>
            {/if}
            {foreach $details as $detail}
            <tr style="background-color: #eee"><th scope="row">{$detail.key}</th>
              <td>{$detail.value}</td></tr>
            {/foreach}
            {if $details}
          </tbody>
        </table>
        {/if}
      </div>

    </div>
  </body>
</html>
